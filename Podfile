# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VenueExplorer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'SDWebImage', '~> 5.0'
  # Pods for VenueExplorer


  target 'VenueExplorerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VenueExplorerUITests' do
    # Pods for testing
  end

end
